apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: chevereto-free-http-mysql-data
  namespace: chevereto-free
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: local-storage
  volumeName: chevereto-free-http-mysql-data
  resources:
    requests:
      storage: 1Gi
