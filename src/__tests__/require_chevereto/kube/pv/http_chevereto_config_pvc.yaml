apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: chevereto-free-http-config
  namespace: chevereto-free
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: local-storage
  volumeName: chevereto-free-http-config
  resources:
    requests:
      storage: 1Gi
