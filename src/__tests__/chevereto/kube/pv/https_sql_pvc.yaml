apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: chevereto-free-https-mysql-data
  namespace: chevereto-free
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: local-storage
  volumeName: chevereto-free-https-mysql-data
  resources:
    requests:
      storage: 1Gi
